int	ft_str_is_numeric(char *str)
{
	int	i;

	while (str[i] != '\0')
	{
		if (str[i] <= 9 || str[i] >= 0)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
	while (str[i] == '\0')
	{
		return (1);
	}
}

